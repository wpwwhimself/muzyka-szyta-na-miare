<header>
    <table>
        <tr>
            <td><img src="{{ URL::asset("msznm.svg") }}" alt="Logo" class="logo"></td>
            <td><h1>{{ $title }}</h1></td>
            <td><img src="{{ URL::asset("msznm.svg") }}" alt="Logo" class="logo"></td>
        </tr>
    </table>
</header>
